import { useState } from 'react';
import toast from 'react-hot-toast';
import api from '../services/api';
export default function AddPassword(){const [form,setForm]=useState({website:'',username:'',password:''});const submit=async(e)=>{e.preventDefault();try{await api.post('/passwords/add',form);toast.success('Password saved');setForm({website:'',username:'',password:''});}catch(err){toast.error(err.response?.data?.message||'Failed')}};return <div className="glass-card p-6"><h2 className="mb-4 text-xl font-bold">Add Password</h2><form onSubmit={submit} className="space-y-3"><input className="input" value={form.website} placeholder="Website" onChange={e=>setForm({...form,website:e.target.value})} required/><input className="input" value={form.username} placeholder="Username" onChange={e=>setForm({...form,username:e.target.value})} required/><input className="input" value={form.password} placeholder="Password" onChange={e=>setForm({...form,password:e.target.value})} required/><button className="btn-primary">Save</button></form></div>}
