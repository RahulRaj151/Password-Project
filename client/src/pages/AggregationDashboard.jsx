import { useEffect, useState } from 'react';
import api from '../services/api';
export default function AggregationDashboard(){const [data,setData]=useState(null);useEffect(()=>{api.get('/students/aggregation').then(r=>setData(r.data));},[]);if(!data) return <div className="glass-card p-6">Loading analytics...</div>;return <div className="space-y-4"><div className="grid gap-4 md:grid-cols-3">{data.groupCounts.map(g=><div key={g._id} className="glass-card p-4"><p className="text-slate-300">Range</p><h3 className="text-2xl font-bold">{g._id}</h3><p>{g.count} students</p></div>)}</div><div className="glass-card p-6"><h3 className="mb-2 text-xl font-semibold">Top Scores</h3><div className="space-y-2">{data.sortedStudents.map(s=><div key={s._id} className="flex justify-between rounded bg-black/30 p-2"><span>{s.name}</span><span>{s.marks}</span></div>)}</div></div></div>}
